<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jessi.omnibus">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.registration.RegistrationActivity" />
        <activity android:name=".ui.login.LogInActivity" />
        <activity android:name=".ui.search.SearchActivityOld" />
        <activity
            android:name=".ui.search.SearchActivity"
            android:label="@string/title_activity_search"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.busselection.BusActivity" />
        <activity android:name=".ui.seat.SeatsActivity" />
        <activity android:name=".ui.passenger.PassengerActivity" />
        <activity android:name=".ui.chekcout.CheckOutActivity" />
        <activity android:name=".ui.payment.PaymentActivity" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.route.RouteActivity" />
        <activity android:name=".ui.coupon.CouponActivity" />
        <activity android:name=".ui.simplify.SimplifyActivity" />
        <activity android:name=".ui.Purchased.PurchasedActivity"></activity>
    </application>

</manifest>